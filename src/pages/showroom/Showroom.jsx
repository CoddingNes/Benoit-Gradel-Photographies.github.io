import React from 'react';
import Navbar from '../../components/navbar/Navbar';

const Gallery = () => {
    return (
        <main>
            <Navbar />
            Gallery
        </main>
    );
};

export default Gallery;